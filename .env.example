PUBLIC_CHAT_API_BASE=http://localhost:4324
# Server-only: base URL of the backend content API used by blog SSR routes.
# Must be set in the production environment (runtime), e.g. the Render backend URL.
BACKEND_API_BASE=http://localhost:4324
