---
import {
  basePathFromPathname,
  languages,
  localizePath,
  ui,
  type Lang,
  type NavKey,
} from '../i18n';

export interface Props {
  activePage?: NavKey;
  lang?: Lang;
}

const { activePage, lang = 'en' } = Astro.props;
const copy = ui[lang].header;
const navLinks = [
  { key: 'about' as const, href: '/about' },
  { key: 'blog' as const, href: '/blog' },
  { key: 'services' as const, href: '/services' },
  { key: 'taxCalculator' as const, href: '/tax-calculator' },
  { key: 'contact' as const, href: '/contact' },
].map((link) => ({
  ...link,
  href: localizePath(lang, link.href),
  label: copy.nav[link.key],
}));

const basePath = basePathFromPathname(Astro.url.pathname);
const flagSvgs: Record<string, string> = {
  us: '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 15" width="18" height="14" aria-hidden="true"><rect width="20" height="15" fill="#b22234"/><rect y="0" width="20" height="1.15" fill="white"/><rect y="2.3" width="20" height="1.15" fill="white"/><rect y="4.6" width="20" height="1.15" fill="white"/><rect y="6.9" width="20" height="1.15" fill="white"/><rect y="9.2" width="20" height="1.15" fill="white"/><rect y="11.5" width="20" height="1.15" fill="white"/><rect y="13.8" width="20" height="1.15" fill="white"/><rect width="8" height="7.5" fill="#3c3b6e"/></svg>',
  es: '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 15" width="18" height="14" aria-hidden="true"><rect width="20" height="15" fill="#c60b1e"/><rect y="3" width="20" height="9" fill="#ffc400"/></svg>',
};
const languageLinks = [
  { lang: 'en' as const, href: localizePath('en', basePath), flag: 'us' },
  { lang: 'es' as const, href: localizePath('es', basePath), flag: 'es' },
];
---

<header class="site-header" role="banner">
  <nav class="navbar" aria-label={copy.navLabel}>
    <!-- Logo -->
    <a href={localizePath(lang, '/')} class="logo" aria-label={copy.homeLabel}>
      <span class="logo-square" aria-hidden="true">LB</span>
      <span class="logo-text-group">
        <span class="logo-name">LB &amp; CO</span>
        <span class="logo-tagline">Global Advisors</span>
      </span>
    </a>

    <!-- Desktop nav -->
    <ul class="nav-menu" role="list">
      {navLinks.map(({ key, href, label }) => (
        <li class="nav-item">
          <a
            href={href}
            class={`nav-link${activePage === key ? ' is-active' : ''}`}
          >
            {label}
          </a>
        </li>
      ))}
    </ul>

    <!-- Actions -->
    <div class="nav-actions">
      <!-- Language switcher -->
      <div class="lang-switcher" role="group" aria-label={copy.languageSelector}>
        {languageLinks.map((languageLink) => (
          <a
            class={`lang-btn${lang === languageLink.lang ? ' active' : ''}`}
            href={languageLink.href}
            hreflang={languages[languageLink.lang].htmlLang}
            lang={languages[languageLink.lang].htmlLang}
            aria-label={copy.switchTo[languageLink.lang]}
            aria-current={lang === languageLink.lang ? 'true' : undefined}
          >
            <span aria-hidden="true" class="lang-flag" set:html={flagSvgs[languageLink.flag]}></span>
            <span>{languages[languageLink.lang].label}</span>
          </a>
        ))}
      </div>

      <!-- Mobile hamburger -->
      <button
        id="menu-toggle"
        class="menu-toggle"
        aria-label={copy.openMenu}
        aria-expanded="false"
        aria-controls="mobile-drawer"
      >
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </nav>
</header>

<!-- Mobile drawer -->
<div id="mobile-drawer" class="drawer" role="dialog" aria-modal="true" aria-label={copy.mobileNavLabel}>
  <div class="drawer-inner">
    <div class="drawer-header">
      <span class="drawer-title">{copy.menu}</span>
      <button id="drawer-close" class="icon-btn" aria-label={copy.closeMenu}>&times;</button>
    </div>
    <nav aria-label={copy.mobileNavLabel}>
      <ul role="list" style="display:grid;gap:0.25rem;margin-top:1.5rem;">
        {navLinks.map(({ href, label }) => (
          <li>
            <a href={href} class="nav-link" style="padding-block:0.9rem;font-size:0.85rem;">
              {label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</div>
